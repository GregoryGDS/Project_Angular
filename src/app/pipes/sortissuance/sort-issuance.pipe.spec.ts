import { SortIssuancePipe } from './sort-issuance.pipe';

describe('SortIssuancePipe', () => {
  it('create an instance', () => {
    const pipe = new SortIssuancePipe();
    expect(pipe).toBeTruthy();
  });
});
